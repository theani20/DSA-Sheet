#include<vector>

int getSingleElement(vector<int> &arr) { 
	int n = sizeof(arr);
	int ans;
	for (int i = 0; i <n;) 
	{
          if (arr[i] != arr[i + 1]) {
            return arr[i];
          } else
            i += 2;
        }
}
